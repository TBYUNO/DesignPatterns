#include "../../../src/behavioral/mediator/Mediator.h"
#include <gtest/gtest.h>

// 中介者模式测试套件
// 注意：由于Mediator.h未提供，这里提供一个基本的测试框架

TEST(MediatorTest, BasicTest) {
    // 占位测试
    EXPECT_TRUE(true);
}
